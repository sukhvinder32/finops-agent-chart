{{- /*
Copyright IBM, Inc. All Rights Reserved.
SPDX-License-Identifier: APACHE-2.0
*/}}

{{- if and (not (empty .Values.cloudability.secret.existingSecret)) .Values.cloudability.secret.create }}
{{- fail "Cannot set both cloudability.secret.existingSecret and cloudability.secret.create" }}
{{- end }}

{{- if and .Values.cloudability.enabled (not .Values.cloudability.secret.existingSecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "cloudability.secret.name" . | trim }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels: {{- include "common.labels.standard" (dict "customLabels" .Values.commonLabels "context" .) | nindent 4 }}
    app.kubernetes.io/part-of: finops-agent
    app.kubernetes.io/component: config
  {{- if .Values.commonAnnotations }}
  annotations: {{- include "common.tplvalues.render" (dict "value" .Values.commonAnnotations "context" .) | nindent 4 }}
  {{- end }}
type: Opaque
data:
  CLOUDABILITY_KEY_ACCESS: {{ .Values.cloudability.secret.cloudabilityAccessKey | b64enc | quote }}
  CLOUDABILITY_KEY_SECRET: {{ .Values.cloudability.secret.cloudabilitySecretKey | b64enc | quote }}
  CLOUDABILITY_ENV_ID: {{ .Values.cloudability.secret.cloudabilityEnvId | b64enc | quote }}
  {{- if .Values.cloudability.secret.customAzureClientSecret }}
  CLOUDABILITY_CUSTOM_AZURE_BLOB_CLIENT_SECRET: {{ .Values.cloudability.secret.customAzureClientSecret | b64enc | quote }}
  {{- end }}
{{- end }}
